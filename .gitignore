# Entorno virtual
venv/
.venv/

# Working copy of the report at the repo root (the committed copy lives in docs/).
/ai_radar_report.csv

# Note: radar.db and docs/ ARE committed on purpose (git-scraping): the snapshot
# history must persist between scheduled runs so momentum can be computed.

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node / Vite (React app in web/)
web/node_modules/
web/dist/
npm-debug.log*

# Generated data feeds (produced by `python -m ai_radar`; the committed copies
# live in docs/ after the Vite build).
web/public/data.json
web/public/ai_radar_report.csv

# VS Code
.vscode/

# Secretos
.env
